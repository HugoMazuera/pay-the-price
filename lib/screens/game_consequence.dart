import 'package:flutter/material.dart';

class Consequence extends StatelessWidget {
  const Consequence({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
